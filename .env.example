# Application Configuration
APP_NAME=Pingo Receipt Parser API
APP_VERSION=0.1.0
DEBUG=false

# Server Configuration
HOST=0.0.0.0
PORT=8000

# Environment
NODE_ENV=production

# Optional: Database Configuration (for future use)
# DATABASE_URL=postgresql://user:password@localhost:5432/receipts_db

# Optional: External Service URLs (for future use)
# LANGFLOW_HOST=http://localhost:7860
# NEXT_PUBLIC_LANGFLOW_HOST=http://localhost:7860

# Optional: CORS Configuration (for future use)
# CORS_ORIGINS=http://localhost:3000,http://localhost:8080

# Optional: Logging
# LOG_LEVEL=INFO