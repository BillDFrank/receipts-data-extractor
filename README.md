# Receipts Data Extractor

Receipts Data Extractor is a Python CLI and library to extract structured data from receipt images and PDFs (vendor, date, total, line items).

Usage:
    rde /path/to/receipt.jpg --output-format json

